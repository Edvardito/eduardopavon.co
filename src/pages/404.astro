---
import NotFound from '~/components/pages/NotFound.astro';
import { FALLBACK_LOCALE } from '~/i18n';

/* Served for any unmatched path, locale or not, so it speaks the fallback. */
---

<NotFound locale={FALLBACK_LOCALE} />
