---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full px-2.5 py-1 text-[0.6875rem] font-medium tracking-[0.08em] uppercase"
  style="border: 1px solid var(--hairline); color: var(--text-muted)"
>
  {label}
</span>
