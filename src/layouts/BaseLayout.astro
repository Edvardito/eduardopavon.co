---
import type { ImageMetadata } from 'astro';
import { Font } from 'astro:assets';
import Footer from '~/components/layout/Footer.astro';
import Header from '~/components/layout/Header.astro';
import Seo from '~/components/seo/Seo.astro';

import { LOCALE_METADATA, t, type Locale } from '~/i18n';
import '~/styles/global.css';

interface Props {
  locale: Locale;
  title: string;
  description: string;
  path: string;
  ogImage: ImageMetadata;
  ogImageAlt: string;
  /** False where the page sets the artist's name at display size itself. */
  masthead?: boolean;
}

const { locale, title, description, path, ogImage, ogImageAlt, masthead = true } = Astro.props;
---

<!doctype html>
<html lang={LOCALE_METADATA[locale].htmlLang}>
  <head>
    <Seo
      locale={locale}
      title={title}
      description={description}
      path={path}
      ogImage={ogImage}
      ogImageAlt={ogImageAlt}
    />
    <!-- Only the display face is preloaded: it sets the largest text on the page. -->
    <Font cssVariable="--font-irregardless" preload />
    <Font cssVariable="--font-polymath" />
    <slot name="head" />
  </head>
  <body class="flex min-h-dvh flex-col">
    <a class="skip-link" href="#main">
      {t(locale, 'a11y.skipToContent')}
    </a>
    {masthead && <Header locale={locale} />}
    <main id="main" class="flex-1">
      <slot />
    </main>
    <Footer locale={locale} />
  </body>
</html>
