# Absolute path to the folder holding the artwork originals, which are never
# committed. Used only by `docker compose --profile tools run --rm images`.
# Forward slashes work on every OS, including Windows.
ARTWORK_ORIGINALS=C:/Users/you/Downloads/eduardopavon-images
